# Copy to `.env` and set your local Postgres connection string.
# `connection_limit` caps how many connections the shared pool opens.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/upvote_api?schema=public&connection_limit=5"

# Optional: HTTP port (defaults to 3000)
PORT=3000
